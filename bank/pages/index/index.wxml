<!--index.wxml-->
<view>
  <view class="top">
    <view class='tips'>
      {{totalNum}}人已集齐哦，您已经集齐{{selfNum}}张花卡
    </view>
  </view>
  <view class='bannerImg'>
     <image class='{{imgsrc?"hide":"bgimg"}}' src="/images/index/bg.png"></image> 
     <image class='{{imgsrc?"cardimg":"hide"}}' src="{{imgsrc}}"></image> 
  </view>
  <view class='bottom'>
    <view class='cards' >
       <block wx:for="{{cards}}" wx:key="{{name}}">
          <view wx:if="{{item.name!=='6'||item.name==='6'&&item.num-0>0}}" class='card' data-badge='{{item.num-0}}'  data-src='{{item.src}}' bindtap='clickNum'>
            <text class='{{item.num-0>0?"badge":"hide"}}'>{{item.num}}</text>
            <image src="/images/index/{{item.num-0>0?'icon_0'+item.name:'icon_00'}}.png"></image>
          </view>
       </block>
    </view>
    <image class='lottery' bindtap='lottery' src='/images/index/button.png'>
    <text class='lottery_text' >抽奖 (剩余{{drawTimes}}次)</text>
    </image>
    
  </view>
  <view class='{{showHelp?"help":"hide"}}' bindtap='changeShow'>
    <view class='helpInfo'>
      <view class='info_title'> 哇哦，花卡与您擦肩而过了</view>
      <view class='info_content'>
        快快分享好友增加抽奖机会吧，使用中国银行手机银行完成任意一笔交易可获取指定花卡哦
      </view>
      <button class='share_btn' open-type="share" bindtap='changeShow'> 邀请好友助力</button>
    </view>
  </view>
</view>