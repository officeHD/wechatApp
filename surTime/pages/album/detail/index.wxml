<view>
  <view class='album'>
    <block wx:for="{{album}}" wx:key="{{index}}">
      <image bindlongtap='showDel' src='{{item.FileURL}}'></image>
    </block>
  </view>
  <view class='add_album' bindtap='add_pic'>添加图片</view>
</view>